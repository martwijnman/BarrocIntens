<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Contracts.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Contracts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BarrocIntens.Data"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="QuoteListView"
           IsItemClickEnabled="True"
           >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Quote">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Customer.Name}"/>
                        <TextBlock Text="{Binding CreatedAt}"/>
                        <TextBlock Text="{Binding ItemsCount}"/>
                        <Button Click="Button_Click_2">Details</Button>
                        <Button Click="Button_Click">Accepteren</Button>
                        <Button Click="Button_Click_1">Weigeren</Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
