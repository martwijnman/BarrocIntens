<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Product.OrderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Product"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    mc:Ignorable="d">

    <StackPanel>
        <ListView x:Name="WalletListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Product.Name}" Width="200"/>
                        <TextBlock Text="{Binding Aantal}" Width="50"/>
                        <TextBlock Text="{Binding Product.Price}" Width="80"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ComboBox x:Name="CustomerCheckbox"
      Header="Customer"
      PlaceholderText="Status selecteren"
      Width="200">
        </ComboBox>
        <Button Content="Order maken" Click="MakeOrder" />

    </StackPanel>
</Page>
