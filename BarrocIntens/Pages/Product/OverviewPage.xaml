<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Product.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Product"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="ProductListView"
                  IsItemClickEnabled="True"
                  ItemClick="RoomItem"
                  >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name }" x:Name="detailSelectorBarItem" />
                        <TextBlock Text="{x:Bind Category}" x:Name="detailSelectorBarItem" />
                        <TextBlock Text="{x:Bind Price}" x:Name="detailSelectorBarItem" />
                        <TextBlock Text="{x:Bind Stock}" x:Name="detailSelectorBarItem" />
                        <TextBlock Text="{x:Bind MinimumStock}" x:Name="detailSelectorBarItem" />
                        <TextBlock Text="{x:Bind Deliverer}" x:Name="detailSelectorBarItem" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
