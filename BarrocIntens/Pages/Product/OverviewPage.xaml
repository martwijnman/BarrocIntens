<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Product.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Product"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BarrocIntens.Data"
    mc:Ignorable="d">

    <Grid Background="#0B0B0B"
      Padding="24">

        <StackPanel Spacing="26">

            <!-- FILTER BAR -->
            <Border Background="#141414"
                CornerRadius="18"
                Padding="16">

                <StackPanel Orientation="Horizontal"
                        Spacing="16"
                        VerticalAlignment="Center">

                    <ComboBox x:Name="StockCheckbox"
                          Width="190"
                          PlaceholderText="Voorraad"
                          Background="#0F0F0F"
                          Foreground="White"
                          BorderBrush="#2A2A2A"
                          BorderThickness="1"
                          CornerRadius="10"
                          SelectionChanged="Filter_Changed">
                        <x:String>Op voorraad</x:String>
                        <x:String>Niet op voorraad</x:String>
                    </ComboBox>

                    <NumberBox x:Name="PriceFilter"
                           Width="150"
                           PlaceholderText="Max prijs....."
                           Background="#0F0F0F"
                           Foreground="White"
                           BorderBrush="#2A2A2A"
                           BorderThickness="1"
                           CornerRadius="10"
                           ValueChanged="Filter_Changed"/>

                    <TextBox x:Name="ProductSearchTextBox"
                         Width="240"
                         PlaceholderText="Zoek product…"
                         Background="#0F0F0F"
                         Foreground="White"
                         BorderBrush="#2A2A2A"
                         BorderThickness="1"
                         CornerRadius="10"
                         Padding="10,6"
                         TextChanged="ProductSearchTextBox_TextChanged"/>

                    <Button Content="Order"
                        Padding="16,8"
                        Background="#FFD600"
                        Foreground="Black"
                        FontWeight="SemiBold"
                        CornerRadius="12"
                        BorderThickness="0"
                        Click="GoToOrder"/>
                    <Button Content="+"
                    Padding="16,8"
                    Background="#FFD600"
                    Foreground="Black"
                    FontWeight="SemiBold"
                    CornerRadius="12"
                    BorderThickness="0"
                    Click="GoCreate_Button"/>
                </StackPanel>
            </Border>

            <!-- PRODUCT GRID -->
            <ListView x:Name="ProductView"
                  Background="Transparent"
                  SelectionMode="None"
                  IsItemClickEnabled="True">

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"
                                   MaximumRowsOrColumns="4"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="14"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">

                        <!-- PRODUCT CARD -->
                        <Border Width="220"
                            Height="480"
                            Background="#141414"
                            CornerRadius="20"
                            Padding="16">

                            <StackPanel Spacing="16">

                                <Image Source="{x:Bind ImagePath}"
                                   Height="140"
                                   Stretch="UniformToFill"/>

                                <TextBlock Text="{x:Bind Name}"
                                       FontSize="17"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       TextWrapping="Wrap"
                                       MaxLines="2"/>

                                <TextBlock Text="{x:Bind Price}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="#FFD600"/>

                                <Border Background="#0F0F0F"
                                    Padding="6,3"
                                    CornerRadius="8"
                                    BorderBrush="#2A2A2A"
                                    BorderThickness="1"
                                    HorizontalAlignment="Left">

                                    <TextBlock Text="{x:Bind Stock}"
                                           FontSize="12"
                                           Foreground="#B5B5B5"/>
                                </Border>

                                <StackPanel Orientation="Horizontal"
                                        Spacing="10"
                                        Margin="0,8,0,0">

                                    <Button Content="Detail"
                                        Width="82"
                                        Background="Transparent"
                                        Foreground="#FFD600"
                                        BorderBrush="#FFD600"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Click="ProductClick"/>

                                    <Button Content="+"
                                        Width="42"
                                        Background="#FFD600"
                                        Foreground="Black"
                                        CornerRadius="10"
                                        Click="PlusClick"/>

                                    <Button Content="-"
                                        Width="42"
                                        Background="#FFD600"
                                        Foreground="Black"
                                        CornerRadius="10"
                                        Click="MinClick"/>
                                </StackPanel>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </Grid>




</Page>
