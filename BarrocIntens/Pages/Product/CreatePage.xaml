<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Product.CreatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Product"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollView  Width="1400">
            <StackPanel
            Spacing="20"
            >
                <TextBox x:Name="NameTextbox"
             Header="Name"
             PlaceholderText="Product name" />

                <TextBox x:Name="CategoryTextbox"
             Header="Category"
             PlaceholderText="Category" />

                <TextBox x:Name="PriceTextbox"
             Header="Price"
             InputScope="Number"
             PlaceholderText="0.00" />

                <TextBox x:Name="StockTextbox"
             Header="Stock"
             InputScope="Number"
             PlaceholderText="Current stock" />

                <TextBox x:Name="MinimumStockTextbox"
             Header="Minimum stock"
             InputScope="Number"
             PlaceholderText="Minimum required" />

                <TextBox x:Name="DelivererTextbox"
             Header="Deliverer"
             PlaceholderText="Supplier" />

                <ToggleSwitch x:Name="NotificationOutOfStockToggle"
                  Header="Notify when out of stock?" />

                <TextBox x:Name="ImageTextbox"
             Header="Image URL"
             PlaceholderText="Image path or URL" />

                <TextBlock x:Name="errorText" />

                <Button Click="CreateButton">
                    Save
                </Button>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
