<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Appointments.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Appointments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BarrocIntens.Data"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="AppointmentListView"
                  IsItemClickEnabled="True"
                  >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Appointment">
                    <StackPanel>
                        <!--<TextBlock Text="{x:Bind Appointment.Customer.Name }" /--><!-->
                        <TextBlock Text="{x:Bind Appointment.Employee.Name }" />-->
                        <TextBlock Text="{x:Bind Date }" />
                        <TextBlock Text="{x:Bind Location }" />
                        <TextBlock Text="{x:Bind Notes }" />
                        <TextBlock Text="{x:Bind Status }" />
                        <TextBlock Text="{x:Bind Type }" />
                        <TextBlock Text="{x:Bind Time }"  />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
