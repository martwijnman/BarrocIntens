<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>


    <Grid Background="#1E1E1E">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>

        <TextBlock 
        Text="Hallo {x:Bind Name}"
        FontSize="32"
        FontWeight="SemiBold"
        Margin="40"
        Foreground="White"/>

        <Grid Grid.Row="1" Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- PLANNING -->
            <Button Grid.Column="0"
                Width="150" Height="150"
                Margin="10"
                CornerRadius="16"
                Background="#2D2D2D"
                Foreground="White"
                Click="PlanningClick">

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE787;" FontSize="50"/>
                    <TextBlock Text="Planning" FontSize="17" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>

            <!-- CUSTOMER -->
            <Button Grid.Column="1"
                Width="150" Height="150"
                Margin="10"
                CornerRadius="16"
                Background="#2D2D2D"
                Foreground="White"
                Click="linkdiewerkt">

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE77B;" FontSize="50"/>
                    <TextBlock Text="Customer" FontSize="17" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>

            <!-- EMPLOYEE -->
            <Button Grid.Column="2"
                Width="150" Height="150"
                Margin="10"
                CornerRadius="16"
                Background="#2D2D2D"
                Foreground="White"
                Click="EmployeeClick">

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="&#xE13D;" FontSize="50"/>
                    <TextBlock Text="Employee" FontSize="17" Margin="0,10,0,0"/>
                </StackPanel>
            </Button>

        </Grid>
    </Grid>

</Page>
