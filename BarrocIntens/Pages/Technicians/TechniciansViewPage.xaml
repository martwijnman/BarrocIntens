<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BarrocIntens.Pages.Technicians.TechniciansViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BarrocIntens.Pages.Technicians"
    xmlns:localData="using:BarrocIntens.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Spacing="10" Margin="5">
            <TextBox x:Name="TechniciansNameSearchTextBox" 
                    PlaceholderText="Zoek op naam..."
                    Width="200"
                    TextChanged="TechniciansNameSearchTextBox_TextChanged"/>

        </StackPanel>

        <ListView x:Name="TechniciansListView" Grid.Row="1" IsItemClickEnabled="True" ItemClick="TechniciansListView_ItemClick">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="localData:Employee">
                    <StackPanel Spacing="10" Padding="20">
                        <TextBlock>
                <Run>(#</Run>
                <Run Text="{x:Bind Id}" />
                <Run Text="{x:Bind Name}" />
                <Run>)</Run>
                        </TextBlock>
                        <Button x:Name="UpdateButton" Click="UpdateButton_Click">
                            <FontIcon FontFamily="Segoe Fluent Icons" FontWeight="Bold" Glyph="&#xE104;" />
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <ListView x:Name="ToolkitListView">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>
